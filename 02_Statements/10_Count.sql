/*
COUNT se encarga de contar cuantos elemetos de un atributo NO ESTAN A NULL.
*/

SELECT COUNT(planetas) from estrellas;

-- Cuenta cuantos elementos hay en total en la tabla registrados.

SELECT COUNT(*) from estrellas;
